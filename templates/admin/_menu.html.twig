{% set adminPage = 'accueil' %}
<nav class="navbar navbar-expand-lg navbar-dark bg-secondary">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a href="{{ path('admin_index') }}"
                   class="nav-link {{ adminPage == 'accueil' ? 'active' }}">Accueil</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('admin_espece_index') }}"
                   class="nav-link {{ adminPage == 'espece' ? 'active' }}">Espèces</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('admin_animal_index') }}"
                   class="nav-link {{ adminPage == 'animal' ? 'active' }}">Animaux</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('admin_personne_index') }}"
                   class="nav-link {{ adminPage == 'personne' ? 'active' }}">Personnes</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('admin_adresse_index') }}"
                   class="nav-link {{ adminPage == 'adresse' ? 'active' }}">Adresses</a>
            </li>
        </ul>
    </div>
</nav>