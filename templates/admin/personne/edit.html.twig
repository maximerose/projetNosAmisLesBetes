{% extends 'admin/base.html.twig' %}

{% block title %}Edit Personne{% endblock %}

{% block admin %}
    <h1>Edit Personne</h1>

    {{ include('admin/personne/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_personne_index') }}">back to list</a>

    {{ include('admin/personne/_delete_form.html.twig') }}
{% endblock %}
