{% extends 'admin/base.html.twig' %}

{% block title 'Ajouter un personne' %}

{% block admin %}
    <div class="container-fluid">
        <h1>Ajouter une nouvelle personne</h1>

        {{ include('admin/personne/_form.html.twig', {button_label : 'Ajouter'}) }}

        <a href="{{ path('admin_personne_index') }}">Retour à la liste</a>
    </div>
{% endblock %}
