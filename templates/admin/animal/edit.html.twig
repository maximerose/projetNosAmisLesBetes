{% extends 'admin/base.html.twig' %}

{% block title 'Modifier l\'animal : ' ~ animal.nom %}

{% block admin %}
    <div class="container-fluid">
        <h1>Modifier l'espèce : {{ animal.nom }}</h1>

        {{ include('admin/animal/_form.html.twig', {button_label : 'Modifier'}) }}

        {{ include('admin/animal/_delete_form.html.twig', {table: false}) }}

        <a href="{{ path('admin_animal_index') }}">Retour à la liste</a>
    </div>
{% endblock %}
