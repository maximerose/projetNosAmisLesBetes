{% extends 'admin/base.html.twig' %}

{% block title 'Modifier le compte : ' ~ user.username %}

{% block admin %}
    <div class="container-fluid">
        <h1>Modifier le compte : {{ user.username }}</h1>

        {{ include('admin/user/_form.html.twig', {button_label : 'Modifier'}) }}

        ${% if is_granted('ROLE_ADMIN') %}
            {{ include('admin/user/_delete_form.html.twig', {table: false}) }}
        {% endif %}

        <a href="{{ path('admin_user_index') }}">Retour à la liste</a>
    </div>

{% endblock %}
