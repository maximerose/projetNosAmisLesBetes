<form method="post" action="{{ path('admin_espece_delete', {'id': espece.id}) }}"
      onsubmit="return confirm('Voulez-vous vraiment supprimer cette espèce?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ espece.id) }}">
    {% if table %}
        <button class="btn btn-link text-danger"><i class="fas fa-trash-alt"></i></button>
    {% else %}
        <button class="btn btn-danger">{{ button_label|default('Supprimer') }}</button>
    {% endif %}
</form>
